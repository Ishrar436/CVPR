# CVPR Mid assignmemt

# Paddy disease image classifier

A CNN model wich is trained and tested  paddy disease dataset

The paddy disease has 10 class of images among them 9 classes are diseases classes and one class is for normal paddy images

The dataset was downloaded from kaggel

There are 2 CNN model trained and tested
both am them are saved in google drive as .pth file
